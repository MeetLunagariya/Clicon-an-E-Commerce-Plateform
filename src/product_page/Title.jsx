import React from "react";

const Title = ({title}) => {
  return (
    <div className="text-[#191C1F] font-medium font-sans text-left text-wrap">
      {title}
    </div>
  );
};

export default Title;
